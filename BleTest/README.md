First somewhat functional BLE service experiment for Pycom's LoPy module.

Open this directory in Pymakr, upload the code to LoPy and use 
e.g. BLE Scanner (android app) to read and write charasteristics.
